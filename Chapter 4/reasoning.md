I need to figure out where Section 4.3 is located in this ~2200-line file, so I'm estimating it's probably around 40-60% through and planning to read a chunk of lines in the middle to find it.

Read buehler2019.pdf L560-899
Read buehler2019.pdf L899-1138

I'm reviewing the complete Bühler framework now, including the previous chapter's conditions (B1)–(B5) and Chapter 3's assumptions (A1)–(A6), which establish that Z is bounded, 𝓗 is convex, and ρ has a half-bounded Fatou property ensuring lower semicontinuity under almost sure convergence.

The task is to write a new section examining whether Bühler's Proposition 4.3 extension from finite to general probability spaces holds under (A1)–(A6), and if not, to construct a counterexample or identify weaker additional assumptions that make it work without relying on (B1)–(B5). I need to investigate whether the strategy itself converges to optimality and whether I can prove convergence of π_M to π using only (A1)–(A6), focusing on OCE risk measures.

I'm setting up the core mathematical framework now, starting with the definition of π(X) as the infimum over admissible strategies of the risk measure applied to the hedged position.

Now I'm examining the monotonicity structure: π_M is decreasing in M and bounded below by π, so the question becomes whether this sequence actually converges to π under the given assumptions. The key constraint is that (A6) enforces nonnegativity of gains W(δ) for every network strategy, which is a strong structural requirement that might help establish convergence. applies directly with D = ‖Z‖_∞ as a finite constant, so the upper semicontinuity of the OCE along almost surely convergent sequences follows automatically. But the real obstruction emerges at step (F1): I need to verify that the near-optimal strategy δ from 𝓗^u actually has integrable hedge ratios.

The issue is that without position limits, the measurable functions g_k defining the strategy aren't necessarily in L^1, which blocks the application of standard results. I'm also reconsidering the counterexample from earlier—it shows that without assumption (A6), the gains W(δ) can go negative, which would allow losses to explode on small events and violate the lower bound constraint.

With (A6) enforcing nonnegative gains, the upper semicontinuity argument in Step 5 requires both a.s. convergence of the gains η^m_k → η_k and control of the costs. The a.s. convergence relies on Hornik's theorem applied in L^1(μ_k), but without integrability of g_k, that path closes off. I'm wondering if assumptions (A1)–(A6) alone can force the needed integrability—maybe through (A4)'s L^0-boundedness of the gain set and the tightness it implies.

The key insight is that even if the strategies themselves aren't integrable, their gains could still be bounded in probability. So I'm considering a truncation approach: define a sequence of truncated strategies by clamping each δ_k at level j, which forces the representing function into L^1(μ_k). Then as j increases, the gains from these truncated strategies should converge a.s. to the gains from the original strategy, since for almost every ω the strategy value is finite and eventually unaffected by the truncation.

Now I need to verify that the truncated strategies stay in 𝓗 and use upper semicontinuity of the risk measure to conclude that ρ(Y_j) → ρ(Y). Then in Step B I need to approximate the bounded target function using neural networks in L^1, extract a convergent subsequence, and get almost sure convergence of the network strategies to the optimal one. But there's a catch — I need the idempotence property H∘η = η to make the induction work, which means when I approximate the constant term in the recursion, the operator H has to fix that approximation.

Actually, there's a cleaner path: instead of requiring idempotence, I can approximate the unconstrained strategy directly by taking its truncation as the target, then use continuity of H to push the convergence through. Since the truncated strategies converge almost surely and H is continuous in each component, the composed strategies H∘δ^m converge to H∘δ^{(j)} almost surely by induction on the time steps — no idempotence needed. So in the truncation case, I can approximate δ^{(j)} directly without needing idempotence, which aligns with Bühler's approach. Now for the convergence: the gains converge through products of almost surely convergent factors, and the costs are controlled by upper semicontinuity of the cost function, giving me liminf Y_m ≥ Y^{(j)} almost surely. The envelope is bounded below by (A6) and (A1), so I apply the upper semicontinuity lemma to get limsup ρ(Y_m) ≤ ρ(Y^{(j)}) ≤ π(X) + ε, then select m appropriately.

Actually, let me verify this works under just (A1) and (A6) with the standing Bühler structure and OCE ρ — I need to check whether the upper semicontinuity lemma requires ρ_ℓ(Y) to be bounded below or π(X) to be finite. If π(X) = −∞, I'd need to show π_M(X) → −∞ instead, which means for any threshold L I can find a truncation δ that drives the value below L. If ℓ is constant, then ρ becomes −∞, which is degenerate. The thesis defines OCE with ℓ convex, nondecreasing, and continuous—entropic and CVaR being the main examples. For the theorem, if ρ ≡ −∞ the convergence holds trivially, but in the non-degenerate case where ℓ is non-constant and convex nondecreasing, the ratio ℓ(t)/t approaches a positive slope κ as t → +∞.

Since ℓ has an affine minorant with positive slope b > 0, I can bound ρ_ℓ(Y) from below using w + E[ℓ(−Y−w)] ≥ w + a + b·E[−Y−w]. The issue is that if b ≠ 1, this expression can diverge to −∞ as w varies unless E[Y] provides control. For CVaR specifically with ℓ(x) = max(x,0)/(1−α), the bound w + E[max(−Y−w,0)]/(1−α) ≥ w holds for bounded Y, but the behavior as w → −∞ needs careful analysis.

When w becomes very negative, −Y−w becomes large and positive, so the expectation grows like (E[−Y]+|w|)/(1−α), giving w(1 − 1/(1−α)) + E[−Y]/(1−α). Since 1 − 1/(1−α) = −α/(1−α) < 0, multiplying by w → −∞ yields +∞. As w → +∞, the risk measure also diverges to +∞, suggesting CVaR of a bounded-below random variable stays above −∞. As w decreases, the expectation E[(−Y−w)^+] grows since −Y−w increases. When Y has E[Y] = +∞ (unbounded positive tail), then E[−Y] = −∞, making E[(−Y−w)^+] = E[−Y] − w diverge to −∞ as w → −∞, which means the CVaR objective w + E[(−Y−w)^+] → −∞ as well. Since the loss Y is bounded below by −B, the quantiles of −Y are all bounded above by B, which means CVaR should also be bounded by B. But I need to think through this more carefully — the quantiles in the worst tail of −Y could still range quite widely, and averaging them might not give me a tight bound.

Actually, I'm confusing myself here. CVaR averages the largest losses, which correspond to the largest values of −Y. Since −Y ≤ B, those largest values cluster near B, not toward −∞, so the average of the worst outcomes should indeed be bounded above by B.

For the entropic risk measure with Y bounded below by −B, the exponential term e^{−λY} is bounded above by e^{λB}, making the expectation finite and the risk measure bounded. Now I'm checking whether this holds more generally for any convex, non-decreasing, non-constant loss function ℓ applied to Y.

But if ℓ has slope less than 1 everywhere—say ℓ(t) = t/2—then the infimum over w becomes unbounded below, giving −∞. This shows that OCE requires a normalization condition: either ℓ(0) = 0 with 1 in the subdifferential at 0, or at least a slope of 1 somewhere. The standard OCE framework from Ben-Tal and Teboulle imposes exactly this, and the thesis follows this approach with continuity and convexity conditions as laid out in the structure document. So the convergence becomes trivial in that case. I'm reconsidering whether I actually need the normalization assumption at all — looking back at the proof, the upper semicontinuity lemma doesn't require ρ > −∞, the monotonicity result π_M ≥ π holds generally, and the epsilon argument works by choosing a near-optimal δ and bounding the limsup. Maybe I can state the theorem cleanly for OCE without invoking the normalization convention. I'm verifying that the zero network satisfies the constraints—each component evaluates to zero when inputs are zero, so the penalty term vanishes and π(X) stays bounded above by ρ(X), which is finite. Since every network class contains the zero network, π_M is also finite for all M. This means finiteness follows automatically from the structural assumptions without needing explicit bounds.

The key insight is that under just (A1) and (A6) with the standing assumptions and the OCE ρ, we get π_M(X) ↓ π(X) on general Ω—value convergence doesn't require (A2)–(A5), which are only needed for attainment. But I should double-check whether this is really as clean as it seems. The admissible strategy set 𝓗 is defined as the image of unconstrained strategies under the composition H∘δ, and I need to verify that (A6) actually holds for network strategies' gains.

Every constrained strategy—including H∘(network) and H∘(truncated)—satisfies W(δ) ≥ 0 almost surely, which is good. Though (A6) is quite strong as a standing assumption (it rules out genuine downside positions), Chapter 3 justifies it through margin constraints absorbed into H and the constant −m absorbed into Z, so we take it as given. The B-counterexample actually violates (A6) since W(δ_0) = δ_0 ξ/2 can be negative.

Now I need to carefully verify the truncation step, which is the key novelty replacing (F1). For a near-optimal δ ∈ 𝓗^u with ρ(X + W(H∘δ)) ≤ π(X) + ε/2, I'm defining a clamping operation that bounds each component of δ_k between −j and j, keeping adaptedness and Borel measurability intact while ensuring δ^{(j)} stays in 𝓗^u.

Since |δ_k(ω)| is finite almost everywhere, for sufficiently large j the clamped version equals the original exactly, so H∘δ^{(j)} converges pointwise to H∘δ and W(H∘δ^{(j)}) converges almost surely without needing upper semicontinuity. The sequence Y_j = X + W(H∘δ^{(j)}) converges almost surely to Y while remaining bounded below by −‖Z‖_∞ from (A6) and (A1).

Now applying the Fatou-type lemma with D = ‖Z‖∞ gives limsup_j ρ(Y_j) ≤ ρ(Y) ≤ π(X) + ε/2, so I can pick j large enough that ρ(Y_j) ≤ π(X) + ε. The target strategy δ^{(j)} is bounded by j with clamped components that are bounded Borel functions in L^1(μ_k). Using Hornik's universal approximation result, neural networks F{k,m} converge to these clamped functions in L^1(μ_k), and the composed strategies δ^m converge to δ^{(j)} in L^1(P) along a subsequence, almost surely.

By continuity of the payoff functions H_k and induction, the composed payoffs H∘δ^m converge to H∘δ^{(j)} almost surely. The gains converge almost surely by the stochastic integral property, while the costs satisfy limsup_m C_T(H∘δ^m) ≤ C_T(H∘δ^{(j)}) almost surely by upper semicontinuity. This means liminf_m Y'_m ≥ Y_j almost surely where Y'_m is the wealth process under δ^m, and since Y'm is bounded below by −‖Z‖∞, applying the upper semicontinuity lemma again gives limsup_m ρ(Y'm) ≤ ρ(Y_j) ≤ π(X) + ε. I need to find m₀ where ρ(Y'{m₀}) achieves this bound. The integrability condition holds since ℓ is finite-valued, so the envelope requirement is satisfied. For the general question about whether the extension works under assumptions A1–A6, the answer is yes — in fact only A1 and A6 are essential for value convergence, with the truncation argument handling what the other assumptions would cover.

What's striking is that no additional assumptions beyond the standing Bühler framework are needed; we don't require the position limits, idempotence, integrability, or cost envelope conditions from B1–B5. But the user is asking for something different — additional assumptions beyond A1–A6 that aren't B1–B5, so I need to reconsider what structural conditions might be necessary.

Now I'm checking whether A6 actually holds for network strategies. A6 is a Chapter 3 assumption about the admissible set 𝓗, and in Chapter 4 we have 𝓗_M ⊂ 𝓗^u with the objective using H∘δ for δ ∈ 𝓗_M, which stays in 𝓗. But philosophically, does A6 apply to the entire image of 𝓗, or is there a gap in how it extends to network strategies?

The issue is that A6 says every adapted process under H has nonnegative gains — a very strong statement. Without constraints (H = id), this would mean every adapted strategy δ satisfies W(δ) ≥ 0 almost surely, which forces a degenerate market. If both δ and −δ are admissible and the wealth process is linear with c=0, then (δ·S)_T ≥ 0 for all adapted δ, which implies (δ·S)_T = 0 almost surely for all δ. This means S must be constant — the price increments are zero almost surely. So A6 only makes sense when there are genuine constraints like margin requirements; without them, the assumption collapses into degeneracy.

Now I'm thinking about how this connects to Chapter 3's story on margin requirements preventing arbitrage. The theorem is honest about its cost: A6 does the heavy lifting to rule out the counterexample, but it severely constrains the market model. There's also a consistency check worth noting — A6 combined with H_k(·,0)=0 ensures that 0 ∈ 𝓗 and W(0)=0, which is reassuring.

I'm now turning to a potential gap in the argument: the pathwise claim that H∘δ^(j)(ω) = H∘δ(ω) for large j. The composition H∘δ is defined recursively at each step k, where the constraint set depends on the history of the composed process itself.

The key insight is that if δ^(j) and δ agree on all components k, then by induction the composed processes must also agree at every step — they're evaluated at identical arguments. And crucially, for almost every ω, there's a finite threshold j₀(ω) beyond which δ^(j)(ω) = δ(ω) everywhere, since each δ_k(ω) is a finite real vector and we're taking a maximum over finitely many components.

For the third potential gap, Hornik's conditions on the activation function — bounded, measurable, non-constant — are all satisfied. The networks are Borel measurable, which ensures δ^m is adapted to the filtration.

Now checking the remaining holes: the clamped representing functions are Borel by composition of Borel functions with continuous clamping, and they're bounded by j so they're integrable. For the usc-Lemma itself, I should verify the proof from the earlier chapter — either restate it fully or use a simpler constant-envelope special case where the loss is dominated by a fixed bound, allowing me to apply reverse Fatou. Now I'm checking whether the expectation E[ℓ(−Y−w)] is well-defined, since Y could potentially fall below the uniform lower bound −B, which would make ℓ(−Y−w) arbitrarily large and push the expectation into (−∞, +∞]. The positive part of ℓ(−Y−w) is bounded when Y has a lower bound like −D, which ensures ℓ(−Y−w) ≤ ℓ(D+|w|) and keeps the expectation well-defined. In our specific application where Y ≥ −B, this condition holds naturally, though the general lemma from the previous chapter only uses E[ℓ(−Y−w)] as an upper bound target without explicitly assuming such a lower bound on Y. The pointwise inequality holds since the positive part of the left side is integrable while the right side's positive part might not be, but that's fine—if it's not integrable then the expectation is infinite, and if it is then monotonicity applies. The lemma checks out. I'll include a self-contained proof of the constant-envelope version in my writeup, noting it as a special case of the earlier result. Now I need to verify whether the argument requires X ∈ L^∞ or just a lower bound, since I need Y_m ≥ −B for the construction. So the key insight is that only lower-boundedness of X matters for the value convergence result—the upper-boundedness assumptions aren't actually needed. This means the theorem holds under just (A1) and (A6), with (A2)–(A5) sitting unused, which is a nice refinement. Combined with Chapter 3's attainment result that does use all the assumptions, I can establish ε-optimality for networks at the attained optimum.

Now turning to strategy convergence: the question is whether the sequence of near-optimal network strategies actually converges to an optimal strategy. The challenge is that π_M may not be attained within the finite-dimensional parameter space 𝓗_M since it's non-compact, but I can work around this by taking near-minimizers δ^M with value at most π_M + 1/M. The harder question is whether these near-optimal strategies converge in some reasonable topology—and in general the answer is no, because the optimizer itself may not be unique or may not exist in the space we're working with.

However, I can use the Komlós theorem here: the gains W(H∘δ^M) are non-negative by A6 and L^0-bounded by A4, so forward convex combinations of them converge almost surely to some limit W̃_∞ in the closure of 𝓒, which equals 𝓒 by A5. This guarantees the existence of some δ* ∈ 𝓗 achieving at least W̃_∞. Then applying the same attainment argument from Chapter 3 to the minimizing sequence itself—which is indeed minimizing for π since the values π_M decrease to π—I can show that ρ(−Z+W(δ*)) satisfies the necessary conditions. is the key insight: convergence happens at the payoff level through convex combinations, not necessarily in the strategies themselves. Let me construct a counterexample where strategy convergence fails—take a trivial market where the stock is constant and there are no transaction costs, making every strategy equally optimal; network minimizers could then alternate between different strategies without converging, even though all yield the same zero payoff.

A less degenerate case uses redundant assets where two identical stocks create multiple perfect hedges, so minimizers could oscillate between (δ,0) and (0,δ) indefinitely. This illustrates that without strict convexity or injectivity conditions, I can't expect the strategies themselves to converge—only their values do.

Now I'm wondering whether strict convexity of the loss function ℓ might guarantee strategy convergence under certain conditions, and whether the optimal terminal wealth becomes unique when ℓ is strictly convex. —so the optimal payoff is unique almost surely when the minimizer w exists and is attained. For entropic risk measures this works out explicitly. Since W is concave in δ (the integral structure makes it linear, and the convexity of the constraint set C_T implies concavity), I need to figure out whether the set of optimal gains is convex or has some other structure.

If the optimal payoff Y* is unique, then W(δ*) should also be unique almost surely among all optimizers. Under strict monotonicity of ρ (when ℓ is strictly increasing), any slack in domination must disappear, which means the gains of optimal strategies coincide almost surely—though the strategies themselves can still be non-unique due to redundant assets or non-injective mappings. The real question is whether I can get convergence of the actual gains W(H∘δ^M) themselves, not just convex combinations, to the unique optimal gain W*. This feels like a standard argument for strictly convex functionals: if both a minimizing sequence Y_M and the optimum Y* are nearly optimal, their midpoint should have value at least π, and strict convexity would force them together.

For the entropic case specifically, I'm thinking about whether e^{−λY_M} converges to e^{−λY*} in L¹ using a Scheffé-type or uniform integrability argument. There's a classical result that if the functional F(Y) := E[e^{−λY}] is strictly convex and I'm minimizing over a convex set, then a minimizing sequence should converge in some sense controlled by the convexity modulus.

Now I'm using the midpoint argument: evaluating F at (Y_M + Y*)/2 gives me E[√(e^{−λY_M} e^{−λY*})], and by strict convexity this is bounded below by m (the minimum) and above by the average of F(Y_M) and F(Y*), which converges to m. Setting U_M = e^{−λY_M} and U* = e^{−λY*}, I can show that E[√(U_M U*)] → m while E[U_M] → m and E[U*] = m, which means E[(√U_M − √U*)²] → 0. This gives me convergence of √U_M to √U* in L², hence U_M converges to U* in probability, and therefore Y_M converges to Y*.

For the entropic risk measure, this means any minimizing sequence of P&L objectives converges in probability to the unique optimal P&L. The key is that Y* exists as a minimizer with F(Y*) = m by attainment (the entropic risk satisfies the required conditions), and I need to verify that the midpoint (Y_M + Y*)/2 is admissible so the convexity bound holds.

Now I'm checking whether the midpoint construction works: using the concavity of W and the convexity of the risk measure, I can show that F evaluated at the midpoint stays above m, and the monotonicity of F (decreasing in Y) gives me the right direction for the inequality. The Cauchy-Schwarz inequality bounds the expectation, and as the approximation improves, the L² distance between the square roots vanishes, which means the near-optimal strategies converge in probability to the optimal terminal P&L under the entropic risk measure—even though the hedge positions themselves may not converge due to asset redundancy.

I should verify uniqueness: if two optimal terminal P&Ls exist, their corresponding utility values satisfy equality in Cauchy-Schwarz, which forces them to be proportional a.s., and the matching expectations then imply they're identical. For strictly convex OCE measures, the argument becomes more involved and requires uniform convexity properties, but the entropic case gives us this clean result.

Now I'm thinking about whether this extends to general strictly convex loss functions — the convergence should hold under additional regularity conditions, though CVaR is a notable exception since it's not strictly convex and genuinely admits multiple optimal allocations. On the strategy side, if assets are non-redundant (meaning the payoff map is injective and we have no costs or constraints), then convergence of gains should imply convergence of the strategies themselves, at least in simple settings like one-period models where the payoff uniquely determines the position.

For multi-period models, it's more subtle — I need backward induction and conditional non-degeneracy of the increments to pin down the strategies from the terminal gain process. Let me focus on a clean one-period result: with d assets, no costs or constraints, and non-degenerate conditional covariance of the returns, the optimal strategy converges in probability. To pin down that v_M → 0, I need to show the conditional distribution of ΔS is non-degenerate. I'll construct a lower bound on the conditional expectation of |vᵀΔS| for unit vectors v — by continuity in v and compactness of the unit sphere, this minimum is strictly positive almost surely, which forces v_M to vanish in probability. Taking expectations shows that (|v_M|∧1)m converges to zero in probability, which means v_M → 0 in probability. For the multi-period case with backward induction, I'm decomposing the terminal gain difference into an F_{n−1}-measurable component plus the contribution from the final period's increment. The key insight is that for the multi-period case, I need the conditional distribution of each increment to be non-degenerate—not supported on any affine hyperplane—and then backward induction establishes the result. But this creates a tension: assumption (A6) with identity market impact forces all self-financing strategies to have zero gains, making the market degenerate, which contradicts what I'm trying to prove without costs or constraints. gains are defined as the constrained positions weighted by price changes, and I need to show that under zero costs and affine non-degeneracy, the optimal positions converge in probability. The key issue is that costs break the linear structure needed for identification—with costs, the terminal payoff doesn't separate cleanly, so I can't identify the limit. The clean proposition is: assuming no costs and affine conditional non-degeneracy, the constrained positions converge in probability to the unique optimal positions.

Now combining this with the entropic uniqueness result: under entropic regularity plus the standing assumptions with zero costs and non-degeneracy, the gains converge to the optimal payoff in probability, which means the constrained positions should converge to the optimal positions. The identification argument shows the limit is Cauchy in probability and measurable with respect to the filtration, satisfying the self-financing constraint, but I need to verify it actually lies in the admissible strategy space. So the key insight is that under affine non-degeneracy and zero costs, the optimal strategy is actually unique: if two strategies both achieve the maximum gain W*, then their difference must sum to zero along the price path, which forces them to be identical by backward induction. This means the limit ζ converges almost surely to δ*, the unique optimal strategy, since η^M generates gains approaching W* and the identification lemma pins down the strategy itself.

Now I'm checking whether these conditions—zero costs plus the affine non-degeneracy constraint—actually admit non-trivial examples where the wealth process stays non-negative for all adapted strategies. So the risk measure ρ applied to admissible gains stays bounded between zero and the negative expectation of the gain, which is positive — meaning the market does allow statistical arbitrage, though the framework explicitly permits this. The standing assumption (A6) essentially forces all admissible strategies to be weakly arbitrage opportunities, which is economically unusual but acknowledged in Chapter 3 as a consequence of margin requirements absorbed into the constraint set Z.

The cleaner interpretation is to state the lower bound on wealth as W(δ) ≥ −m rather than W ≥ 0, then absorb m into Z by cash-invariance — this gives the same envelope bound on the envelope of Z. For our purposes in Chapter 4, since X = −Z is fixed, if (A6) holds in the W ≥ −m form, the envelope remains constant. Now I need to check whether the optimal portfolio π_M actually exists.

Since the theorem does work, there's no counterexample to provide — but I should verify this against the earlier counterexample I had in mind, which fails both (A6) and (A1), so that's consistent. The real question is whether (A1) and (A6) alone are sufficient, since I haven't actually used (A4), (A5), (A2), or (A3) yet. Value convergence requires...

Actually, let me trace through the full argument: the monotone lemma gives me structure, the truncation step uses (A6) for truncated strategies plus (A1) for the envelope and the upper semicontinuity lemma, Hornik's result handles the bounded target, I get an almost sure subsequence, constraint continuity follows from Carathéodory's theorem, the cost is upper semicontinuous by standing assumptions, and the upper semicontinuity lemma applies again to network strategies using (A6) and (A1). The hidden assumptions are that ρ is an optimal control expectation with a finite convex nondecreasing loss function (continuity is automatic), the activation is measurable, bounded, and non-constant, and the filtration is generated by the information structure — all of which are standing assumptions for this setting.

Now for the technical details: Doob–Dynkin gives me exact representations δ_k = f_k(I₀,…,I_k), and truncation commutes with this representation. Hornik's result assembles components into ℝ^d-valued networks as before, and the subsequence extraction uses L¹ convergence at each step to pull out an almost sure convergent subsequence.

For the exhaustion property, I need δ^m to lie in the appropriate function class for sufficiently large M, using the zero-weight embedding for feedback as in the previous chapter. The key inequality limsup_m C_T(H∘δ^m) ≤ C_T(H∘δ^{(j)}) follows because the cost functions are upper semicontinuous in their arguments, so the pointwise limsup of the costs is bounded by the cost at the limit, and summing preserves this inequality.

Now checking the liminf bound for Y'_m: since Y'_m = X + G_m − C_m, taking liminf gives X plus the limit of G_m minus the limsup of C_m, which recovers Y_j. The convergence of π_M to π is established using only assumptions A1 and A6 along with the structural OCE conditions. I should verify whether both assumptions are actually necessary—the earlier counterexample violates both, but I need to check if dropping just one breaks the result. For instance, if (A6) holds but Z is unbounded above, the gains remain nonnegative but... So the key insight is that with all conditions A1–A6 in place, convergence holds, so I don't need a counterexample there. But I should note where the conditions are sharp — particularly that A1 can be relaxed to just requiring Z bounded above, which is equivalent to the envelope condition E[ℓ(Z+c)]<∞. Without A1, even with A2–A6 satisfied, the approximation can break down, and I'm sketching a concrete example: a single-period setup where the price change is nonnegative almost surely, the hedging strategy clamps the position to [0,1], ensuring all constrained gains stay nonnegative per A6, and checking whether A2 holds in this construction. The measurability issue is tricky since Y/ΔS is F₁-measurable rather than F₀-measurable, so I need to either take a conditional expectation version of η₀ or pick a setup where condition (A5) holds more directly. For the counterexample, I'm checking whether the optimal hedging strategy actually requires unbounded positions when Z is unbounded—but with non-negative gains and Z fixed, hedging can only increase the position, so I need to examine how the risk ρ(−Z + η₀ΔS) behaves as the exponential moment grows. Constants are representable as neural networks through affine transformations, so the full position η₀=1 is achievable exactly. The challenge is that the optimal strategy needs to be non-constant and delicate—a function g(I₀) that networks can only approximate in L¹, but this approximation causes the objective to potentially explode since the envelope −Z isn't integrable and uniform convergence fails.

Now I'm constructing a specific counterexample where the moment generating function E[e^{λ(Z − gΔS)}] is finite for the target but infinite for every network approximation. The idea is to set up Z and the reward spread ΔS so that errors concentrate precisely where Z is huge, forcing η₀ to match g exactly on the bad set—but since networks can only approximate g, they'll fail to control the exponential moment. So the key tension is that networks with continuous activations can approximate g uniformly but can't match it exactly—yet they could actually overshoot g, and if g stays below 1 − ε in the relevant region, clamping at 1 lets the network stay above g while the overshoot doesn't blow up the exponential risk since the excess gets absorbed into the V term.

To truly penalize deviations in both directions I'd need a two-asset setup where one asset pays off when ξ = 1 and another when ξ = −1, forcing η to satisfy both η¹ ≥ g and η² ≥ 1 − g simultaneously—but this runs into the constraint that payoffs must be non-negative, which blocks the negative payoffs needed to actually punish overshooting.

Now I'm realizing that under assumption (A6), upward errors are actually harmless since gains are monotone in the position, while downward errors are bounded by the envelope. The real explosion risk comes from Z's own tail behavior—I need to check whether there exists at least one strategy with finite expected exponential loss, because if every competitor including the good ones has infinite risk, then the network approximation will fail to converge.

The key tension is that networks can overshoot pointwise where they're close in L¹ norm, which helps, but on the bad sets where the network falls far below the target, the clamping constraint η ∈ [0,1]^d limits how much the weight function W can deviate from the optimal W(η*). So the deviation in W is bounded by the sum of individual coordinate deviations times the step sizes, which scales with the network approximation error.

On these bad sets B_m where the network undershoots, the tail of Z could explode—the exponential moment E[e^{λZ}1_{B_m}] might remain infinite even as P(B_m) → 0, since Z itself isn't integrable. This suggests a potential counterexample where the network's undershoot on a shrinking bad set still causes the exponential moment to blow up. But the real question is whether we can choose the approximating sequence carefully enough to avoid this problem, perhaps by using uniform convergence on compact sets rather than just L¹ convergence. So the key insight is that when V depends deterministically on U—say V(u) = 1/u—the finiteness condition becomes a requirement on how closely η must approximate g near zero. A constant network η ≡ 1 can actually achieve this if g stays bounded below 1 with some margin, making the approximation feasible despite g's potential wildness.

But there's a critical problem: since overshoot is essentially free under the constraints, the maximum position strategy dominates trivially because the gain increases monotonically with η₀. To break this dominance and create meaningful trade-offs, I'd need the gains to be non-monotone in η, yet the constraints force all admissible η to yield non-negative gains, which seems to lock me into monotonicity. So if ΔS ≥ 0 across all constrained η₀ ∈ [0,1], the objective becomes monotone decreasing in η₀, pushing the optimum to the boundary at η₀ = 1—a trivial corner solution. But multi-period and multi-asset settings introduce genuine complexity: the constraint Σ η_k ΔS_{k+1} ≥ C_T must hold for all feasible positions, and transaction costs that penalize position changes create nonlinearities that break the corner-optimality pattern, forcing interior solutions with truly functional strategies.

Now I'm checking whether costs can be bounded consistently with the constraint—if price moves are at least 2 per step and proportional costs stay below 1 per step, total costs scale with the number of periods, but the zero strategy trivially satisfies the constraint since it generates zero gains and zero costs. So the gain from η₀ΔS₁ is at least 2, which makes W ≥ 0. More generally, the total variation of η is bounded by twice the sum of its values, and since the gains from the weighted steps scale with Σ η_k ΔS_{k+1} while costs are controlled by the total variation, the net welfare stays non-negative. Now I'm thinking about what happens if we drop assumption (A1). A counterexample seems plausible but constructing one with the remaining assumptions (A2)–(A6) plus unbounded Z turns out to be tricky due to monotonicity issues. The key insight is that without (A1), we can weaken it by replacing boundedness with a moment condition—requiring E[ℓ(Z+c)] < ∞ for all c, which is similar to condition (B5) but with a = 0. Since gains are non-negative, that term vanishes anyway, and the envelope argument still works with D := Z and Y_m ≥ −Z satisfying the needed integrability. This gives a cleaner weakening of (A1). So I should state the main theorem under (A1) and (A6), then note the (A1′) refinement separately. The key comparison with (B2)–(B4) is that position limits, idempotence, cost envelopes, and normalization all become unnecessary here—(A6) handles everything. Need to verify the degenerate case where π could be −∞, and confirm π_M stays finite for all M while the sequence converges in [−∞,∞).

Under Chapter 3's framework where the attainment theorem applies, I can express π(−Z) = ρ(−Z + W(δ*)) which also lives in [−∞,∞), allowing −∞ as well. I should add a remark: when ℓ(t) ≥ t (normalization), there's a lower bound on π(X), and with (A6) and Z ∈ L^∞, I need to check whether ρ_ℓ(Y) respects the bound −‖Z‖_∞ for normalized ℓ.

The issue is that even with normalization, π could still be −∞ because unbounded gains make the expectation E[ℓ(−Y−w)] potentially infinite, which means −E[Y] could be −∞ and thus useless as a bound. This suggests that under (A6) alone, finiteness isn't guaranteed—statistical arbitrage or irrelevant markets are allowed. So the clean statement is convergence in [−∞,∞) without claiming finiteness. I'll note that π > −∞ if and only if the market is relevant, which is orthogonal to the approximation question. Now I'm turning to CVaR to see how this plays out there.

For the strategy convergence section, I need to address two points: first, that values converge but strategies need not—a constant market with redundant assets gives non-unique minimizers that can oscillate, and for CVaR specifically, the loss function isn't strictly convex so multiple strategies can yield the same CVaR if their payoffs differ only above the VaR level. Second, I'll use the full set of assumptions (A1)–(A6) with a Komlós-based argument to establish a positive near-optimality result.

The key insight is that when we take near-optimal network strategies and form forward convex combinations of their gains, these combinations converge almost surely to some limit payoff. By the regularity assumptions, there exists an optimal strategy whose gain dominates this limit, and by Fatou's lemma we can show this strategy is indeed optimal. So the proposition states that network minimizing sequences are themselves "attaining" sequences whose limits recover optimal payoffs.

Now I'm working through the entropic risk measure case with the uniqueness and convergence properties. The main technical hurdle is verifying that the entropic risk ρ_λ stays bounded below—since the exponential is always positive, its expectation is strictly positive, which means the logarithm is well-defined and finite. The value function π, being an infimum over strategies of these finite risk values, also stays above negative infinity.

But there's a subtlety: if the gains become unbounded, the expectation of the exponential could approach zero, pushing the entropic risk toward negative infinity. This means π itself could be −∞, which would make the uniqueness question moot. The attainment theorem from Chapter 3 actually handles this case—when the value is −∞, they construct a sequence of strategies whose objectives converge to −∞, and by Komlós's theorem extract a convergent subsequence whose limit achieves this −∞ value. So the optimum is "attained" in that sense, though uniqueness breaks down.

For the uniqueness argument to work, I need to assume π is finite. Now I'm setting up the proof carefully: I'll define m as the exponential of λ times π evaluated at −Z, which stays in (0,∞). The optimal Y* from Chapter 3 satisfies the moment condition E[e^{−λY*}] = m. Then I construct a sequence Y_M using the constrained strategies, and their exponential moments m_M converge to m as the values approach π. The key step is checking that the midpoint of two constrained strategies stays in the constraint set—since the constraint set is convex by assumption A2, the average of H∘δ^M and δ* lies in 𝓗.

Now I'm verifying that W is concave so the midpoint inequality holds. This requires C_T to be convex, which Chapter 3 established as a standing assumption for the attainment theorem. I need to confirm that C_T(δ) = Σc_k(δ_k − δ_{k−1}) is indeed convex when each cost function c_k(ω,·) is convex.

Once that's settled, I can apply the exponential inequality: since W(midδ) ≥ (W(η)+W(δ*))/2 by concavity, the exponential moment bound follows, and monotonicity of the exponential gives me the desired inequality for m. λ(Y_M + Y*)/2 gives me the geometric mean relationship through the exponential form. Applying Cauchy-Schwarz bounds the expectation of the geometric mean, which forces the L² distance between √U_M and √U* to vanish, establishing convergence in probability and ultimately Y_M converging to Y* in probability through the continuous transformation.

The uniqueness of Y* follows from the equality condition in Cauchy-Schwarz: if two optima exist, they must satisfy the same expectation constraint, which forces them to be proportional with coefficient 1, making them almost surely equal. The gains W(H∘δ^M) then converge to W(δ*) in probability without needing convex combinations for entropic risk.

Now I'm moving to the identification proposition, which requires zero costs and conditional non-degeneracy—specifically, for each time step k, the conditional distribution of the next price change given the information up to time k must be non-degenerate almost surely.

For the backward induction step, I need to establish that the difference G_M − G* decomposes into an F_{n−1}-measurable component A_M plus a term involving the price change, and show both converge to zero in probability. The key condition is that the conditional law of the price change vector, when augmented with a constant 1, doesn't lie in any hyperplane through the origin of the extended space.

To make this rigorous, I'm constructing a jointly measurable kernel representation where for each state ω, the conditional distribution of the price change is captured by a regular conditional distribution. This lets me define a function g(a,v,ω) as the conditional expectation of the truncated inner product, which is continuous in the coefficients (a,v) by dominated convergence. The crucial point is that the assumption—that the conditional law isn't supported on any affine hyperplane—translates to g being strictly positive for all nonzero (a,v), but I need this to hold almost surely simultaneously across the uncountable set of possible (a,v) pairs, not just for individual ones.

Now I'm defining a minimum value m(ω) over the unit sphere, which is positive almost surely since g is continuous and positive on this compact set. This minimum is measurable by taking the infimum over a countable dense subset. Then for the martingale increment, I'm setting up the normalized direction u_M and radius r_M, and showing that the truncated inner product is bounded below by a product involving r_M and the directional component. Since the convergence in L¹ implies convergence in probability and m is positive almost surely, r_M must converge to zero in probability, which means A_M and the coefficient sequence both vanish. Working backward through the recursion shows all coefficients go to zero, establishing uniqueness of the optimal strategy. The proposition then follows: under entropy conditions with the regularity assumptions and non-degeneracy, the optimal strategy is characterized uniquely.

Now I'm checking whether the optimal positions are unique within the constrained set 𝓗, where the elements themselves represent the allowed positions, and verifying that the empirical strategy converges to the true optimum in probability. With c ≡ 0 and the non-degeneracy condition, all constrained gains must be non-negative and the conditional distributions must be full-dimensional, so I need to verify consistency: the terminal gains η·S_T are non-negative for all η in 𝓗, which is a compact convex set containing zero.

For the one-period case with K = [0,1], if η₀ΔS ≥ 0 for all measurable η₀ in that interval, then ΔS must be non-negative almost surely, and the non-degeneracy requirement—that the conditional law of ΔS given F₀ isn't concentrated on an affine hyperplane—is satisfied (for instance, an exponential distribution works). This is consistent, and the corner solution η₀ = 1 becomes the unique optimum, which the networks converge to. For multiperiod settings with costs excluded for identification purposes, the analysis becomes more complex with alternating η strategies.

Now I'm checking whether the entropic proposition holds when values ρ(Y_M) converge to π, examining near-minimizers in the constrained space.

I should also note that without strict convexity or non-degeneracy assumptions, only a Komlós-type convergence result remains, and strategies may oscillate between different solutions. Regarding the user's earlier questions about attainment and convergence to optimality—those are addressed by the analysis above. For π_M itself, while the parameter space is finite-dimensional, it's not compact, so I won't claim attainment but rather that near-minimizers suffice, which I can mention briefly.

Now I'm planning the structure of the new self-contained report: I'll start with a recap of the setting and standing assumptions (restating A1–A6 from earlier chapters), then explain how these assumptions fix the two main failures—truncation handles one issue while the constant-envelope condition addresses the others. I'll include the upper semicontinuity lemma with its full proof and the truncation lemma showing that bounded strategies are value-dense under A1 and A6, which generalizes the one-period result from before.

The key insight is that truncation failed previously because truncated gains weren't dominated and risk could explode, but A6 provides a uniform lower bound that makes the usc-lemma applicable—this connects nicely to the earlier remark about multi-period truncation. Then I'll present the main theorem showing convergence of π_M(−Z) to π(−Z) under the standing setting plus OCE and A1–A6, with corollaries for entropic and CVaR cases that require no additional moment conditions.

Now looking at how A2–A5 turn out to be unnecessary for value convergence, and how the single hypothesis A6 replaces all those earlier envelope conditions like position limits and integrability. The counterexample from before violates both A1 and A6 as expected, and there's a variant where weakening A1 to A1′ with a boundedness condition on A6 still works. Importantly, no counterexample exists under the full A1–A6 framework, so the chapter's main question gets a positive answer.

Moving into strategy convergence now, starting with a negative example showing non-uniqueness prevents convergence, then applying the Komlós proposition with the full A1–A6 to establish that forward convex combinations of network gains converge.

s. to a limit dominated by the optimal gain, recovering the Chapter 3 optimum for minimizing sequences. The entropic case pins down uniqueness of optimal P&L and convergence in probability for network P&L through Cauchy–Schwarz and parallelogram arguments. Position identification follows from zero costs and conditional non-degeneracy, yielding unique optimal positions and convergence of the network strategy to the optimal one in probability via conditional kernel and backward induction. The section wraps up with remarks on CVaR non-uniqueness, how costs disrupt identification, and what questions remain open.

I should also address the framework and open questions—rates of convergence, strategy convergence when costs are present, and what happens if we drop assumption A6, which would fall back to the previous chapter's framework. One thing to verify: the truncation lemma still holds with costs included. When we truncate the strategy sequence, the costs do change, but we showed pointwise eventual equality—for large enough j the entire path matches, so the cost processes are eventually equal too. The upper semicontinuity lemma doesn't require strict inequality, just liminf ≥ limit, so equality works fine. The truncated strategies stay adapted and bounded by j as required.

Now checking the upper semicontinuity lemma applied twice: for the truncated targets Y_j, the envelope is X plus a bounded term from the composed function, which is at least −‖Z‖∞, and the composed strategy stays in the function class. The liminf of Y_j equals Y, so the limsup of the risk functional applied to Y_j is at most the risk of Y. Everything checks out, but I still need to verify that the risk of −Z is finite.

For the zero network case, the risk equals the risk of X since the composed function at zero is zero and the cost function at zero is zero, giving me ρ(X) bounded by w plus the expected loss of the clipped Z, which is finite. Now moving to the monotone lemma showing the nested structure of function classes.

The risk values form a decreasing sequence π_M ≥ π_{M+1} ≥ π. For the entropic P&L convergence result, I need to verify which assumptions are actually required: the Cauchy-Schwarz argument needs π to be finite and an optimizer to exist (which Chapter 3 establishes under A1–A6, and the entropic case satisfies A3), plus convexity of both the function class and the cost function, and monotonicity. The attainment conditions from A1–A6 are necessary, but A4–A6 aren't directly needed beyond ensuring that attainment holds.

The convergence result applies more broadly—any minimizing sequence in the function class converges to the optimizer in probability, not just network-based ones, so I should state it generally and then apply it to networks through the main theorem. For identification, since gains equal negative costs plus the function values, the convergence G_M → G* in probability follows directly from Y_M → Y* in probability. The backward induction decomposition expresses the gain difference as a sum over time steps involving the differences between the learned and optimal value functions. Now I'm decomposing T_M into a measurable component A_M and the term v_jᵀΔS_{j+1}, then applying the non-degeneracy assumption to show that the conditional distribution of the increment doesn't concentrate on any affine hyperplane, which ensures g_j(a,v,ω) > 0 for non-zero (a,v). Now I'm establishing that the bound on the conditional expectation gives me a lower bound on r_M in probability, and since m_j is strictly positive almost surely, the convergence of the product to zero forces r_M itself to converge to zero in probability, which means both components A_M and v_{M,j} must vanish.

For the conditional expectation calculation, I'm verifying that freezing A_M and v_j as F_j-measurable quantities and integrating over the distribution κ_ω is valid. Then I'm checking uniqueness of the optimizer positions by examining what happens when two different portfolio allocations δ¹ and δ² both achieve the same optimal P&L value Y*.

Now I'm thinking about whether the convergence Y_M → Y* in probability holds more generally for strictly convex loss functions beyond the exponential case. The entropic argument relied on multiplicativity, but I'm wondering if uniform convexity on bounded sets could give the same result—the idea being that the midpoint inequality from strict convexity should force convergence. The issue is that the arguments −Y−w aren't bounded above since Y can be arbitrarily large, so I need uniform convexity on half-lines rather than compact intervals. This breaks down for strictly convex losses that flatten at −∞, like entropic losses, which rely on multiplicative structure instead. So for general strictly convex ℓ, I can only get convergence in a weaker sense.

To keep things clean, I'll state the uniqueness result only for entropic losses and remark that for general strictly convex ℓ with an attained optimal w*, the optimal P&L Y₁ + w₁ = Y₂ + w₂ almost surely wherever ℓ is strictly convex. For existence of the optimal w, I need coercivity conditions like lim_{t→∞}ℓ(t)/t > 1, which entropic and CVaR losses satisfy. When both strategies yield the same CVaR, the optimal P&L becomes non-unique, which means minimizing network sequences can oscillate between them—a clean example even on finite probability spaces. I should also note that the network outputs themselves don't necessarily converge even when their compositions do, since H might not be injective. The theorem achieves convergence of the value functions using only assumptions A1 through A6, which I want to emphasize. For the counterexample requirement, since the main result holds, I could instead show that A6 alone without A1 fails by constructing a case where Z violates the modified condition. For the finite-risk condition, I need E[e^{Z−V}] < ∞, which constrains Z ≤ V plus an integrable term. If I set Z := V·g(U) where V is independent with infinite exponential moments, then the expectation factors as an integral over U, and this is finite only when g(u) ≤ η₀(u) almost everywhere—any positive deviation makes the moment infinite. So the feasible strategies are exactly those where η₀ dominates g, and I'm looking for the optimal one that minimizes the exponential loss.

Since η₀ ≡ 1 always satisfies η₀ ≥ g (given g ≤ 1), this constant network achieves finite risk, which means the minimax value π_M is finite. The objective decreases monotonically in η₀, so η₀ ≡ 1 is optimal, and any sequence of networks converging to this strategy will achieve the minimax value—monotonicity prevents any counterexample from arising. To get an interior optimum instead, I'd need an upside penalty, but with non-negative gains and monotone ρ that seems blocked unless I consider multi-agent effects.

With multiple assets and linear gains under monotone ρ, the objective becomes convex but monotone along directions that increase gains, pushing optima to corners. Under constraint (A6), if the gain vector ΔS has non-negative components, then the objective is monotone in each η-component, forcing the optimum to a corner of the feasible set K. For a compact convex K, condition (A6) essentially requires ΔS to lie in the polar cone of K.

To avoid corners, I need the objective to be non-monotone over K. The exponential objective E[e^{Z − ηᵀΔS}] is convex in η, and its unconstrained minimizer occurs where the gradient vanishes—that is, where E[ΔS e^{Z−ηᵀΔS}] = 0. But when ΔS is componentwise non-negative and nonzero, this expectation is strictly positive, making the gradient strictly negative and the objective strictly decreasing, so no interior minimum exists.

This means the optimizer hits a corner—the maximal element of K. If K has a unique maximum like [0,1]^d does at (1,…,1), the optimal strategy is constant and trivially approximable. However, when K's maximal face is curved, like a disk, the geometry becomes more complex. I'm now considering K as a ball centered at c with radius r, and checking when the dual cone condition ΔS ∈ K* holds, which requires ηᵀΔS ≥ 0 for all η in K. In the two-dimensional case with specific ΔS values, I'm exploring how this constraint shapes the feasible region.

The optimal boundary point η* depends on the distribution of directions u in the cone C, and since u may depend on the information I₀, the optimal point becomes a non-constant function G(I₀) on K's curved boundary. This means networks might struggle to approximate G exactly, especially since deviating from η* along the boundary can decrease the gain in unbounded-loss scenarios, even though the gains themselves stay non-negative under condition (A6).

Now I'm looking at how the loss is bounded by Z's tail behavior — the objective E[e^{Z − ηᵀΔS}] is bounded by E[e^Z], but if E[e^Z] is infinite, then each η might also have infinite risk unless ηᵀΔS ≥ Z − C for some e^-integrable C. The key insight is that I can construct a loss function where η* is a wild, boundary-valued function with no continuity properties that networks can approximate, forcing them to fail at capturing the tail-covering requirement.

For a continuous η to beat this, it would need to satisfy (η* − η)ᵀv ≤ ε for all v in the cone C of directions, which means η must be within ε of η* in the dual order — but since both η and η* are constrained to K and η* sits at the maximal boundary, moving upward is impossible, so the gap persists.

Now I'm seeing that if η* is actually discontinuous with essential oscillations everywhere-dense — like a wild function parametrized by angle that jumps by a fixed size ε₀ in every interval — then any continuous network F would necessarily miss G by at least ε₀/2 on a set of positive measure, since F can't match the oscillatory behavior of G.

But here's the key issue: by Lusin's theorem, continuous functions are dense in L⁰(μ₀), so for any measurable G there exist continuous approximations F_n converging to G almost everywhere. The deviations occur only on shrinking sets D_n with μ(D_n) → 0. However, on these deviation sets where |F − G| ~ ε₀, the exponential moment E[e^{V·ε₀·c}1_{D_n}] blows up because V has all exponential moments infinite — the product of an infinite moment with even a small probability gives infinity. This means every continuous strategy ends up with infinite risk, while η* itself has finite risk.

Actually, let me verify η* more carefully. The exponential moment E[e^{Z − ηᵀΔS}] equals E[e^0] = 1 since Z is defined as η(I₀)ᵀΔS exactly, so they cancel. Z is nonnegative but unbounded — it behaves like V times other stuff — which means assumption (A1) is violated. This is precisely the point: I'm constructing this as a sharpness example to show what happens when we drop (A1). For assumption (A6), I need to check that all strategies η in K satisfy ηᵀΔS ≥ 0, which holds when K is appropriately constrained.

Now I'm thinking about whether neural networks with measurable activations can represent the target function. With merely measurable (not continuous) activations, the networks themselves become measurable but not continuous — a much smaller function class. They're dense in L¹ but each network is a specific finite composition of affine maps and the fixed activation. The question is whether this class can match the target function G on the tail-relevant sets exactly, and in general it can't, though proving all networks fail is difficult. The previous chapter's approach sidestepped this by using bounded networks: if the activation is bounded, every network is bounded, so an unbounded target creates a gap on the high-value sets.

Here the issue is different — the networks are bounded in the compact domain K, so deviations are bounded by the diameter of K. To make the argument work, I need V to have infinite tails for all positive ε, which lognormal V satisfies since E[e^{εV}] = ∞ for every ε > 0. But with measurable activations, I can't rely on continuity arguments, so I need to show that any function in the network class fails to represent G. The network class is countable-dimensional and includes compositions with bounded measurable activations like indicator functions, which could potentially represent piecewise-constant functions matching a pathological G.

The key constraint is that finite risk requires (G − F)⁺ ≤ h_u where h_u is the slack term C(ω)/V. With the exponential moment condition and V independent, I need E[e^{c(u)V}] to be finite, but since E[e^{tV}] = ∞ for all t > 0, the only way this works is if (G − F) times the relevant direction is nonpositive almost everywhere — meaning zero slack is required. This forces F to dominate G in the cone order while staying in K, and since G sits on the maximal boundary, F must equal G almost everywhere. So the counterexample exists precisely when G isn't representable by any network in the class.

Now I'm checking whether cardinality arguments work here. The network class is parametrized by finitely many reals, giving it continuum cardinality, and measurable functions modulo null sets also have continuum cardinality, so a naive diagonal argument doesn't apply. Instead, I can construct an explicit G by fixing a measurable activation function and noting that each network is a composition of weight matrices and activations. The full network class is a countable union of continuum-parametrized families indexed by architecture, so I need to show there exists a measurable function outside this union.

I'm considering whether to force networks onto a strictly convex boundary curve to create a contradiction, but that's getting messy. It might be cleaner to just state the sharpness result for continuous activations only, since that's where the previous chapter's counterexample already worked — they showed unboundedness of the target defeats bounded activations and continuity defeats continuous ones, whereas here I'm only using the continuity property. so the sharpness remark shows that even with continuous bounded activation functions, conditions (A2) through (A6) can all hold while the complexity π_M stays infinite and exceeds π — which means (A1) or (A1′) is essential and can't be dropped. I'm verifying each condition: the hypothesis class is convex since K is convex and the metric projection onto K is Carathéodory measurable, entropy is satisfied, and the gains are bounded by a single dominating term RV.

For condition (A5) on closedness of the set {Y ≤ η₀ᵀΔS}, I need to handle the limit of Y_n = η_nᵀΔS − L_n converging in probability. Since the η_n are bounded K-valued F₀-measurable sequences, I can apply Komlós's theorem to extract a.s. convergent convex combinations η̃_n → η∞, which preserves the F₀-measurability and K-valuedness, then use these convex combinations to construct the corresponding limit for Y. Since η̃_n converges almost surely to η∞, the product η̃ᵀΔS converges almost surely as well, which means L̃_n converges in probability to η∞ᵀΔS minus Y, and this must be non-negative almost surely, so Y is bounded above by η∞ᵀΔS. Now I'm setting up a concrete sharpness example with d = 2 and a single period to demonstrate that the construction satisfies all conditions except (A1).

For this example, I'm taking the initial information U uniformly distributed on (0,1), then introducing direction Θ and magnitude V in the second period, where the price change ΔS is the magnitude times a unit vector in direction Θ. The constraint set K is a quarter-disk in the positive orthant—convex and compact with the origin included. Since both the portfolio weights and the direction vector have non-negative components, their inner product is automatically non-negative, which verifies condition (A6).

Now I'm confirming that the projection onto K is continuous with the origin mapping to itself, and costs nothing. The optimal strategies concentrate on the boundary arc where the norm equals one. I'm constructing a target strategy G(U) = u(φ(U)) where φ maps (0,1) into [π/6, π/3] in a way that's measurable but nowhere continuous—specifically using a fat-Cantor-type set that's dense in every subinterval so that φ oscillates between the endpoints without settling into any continuous pattern.

The payoff Z = G(U)ᵀΔS becomes a product of V and the inner product of G and u, both constrained to the cone angle range, which guarantees their dot product stays bounded below by √3/2, making Z at least (√3/2)V and therefore unbounded with no exponential moments. This violates both the boundedness and exponential moment conditions, confirming the theorem's failure. Under entropic risk with λ = 1, the risk of −Z is zero since the risk of −Z plus the correction term equals the risk of zero.

Now I'm establishing that for any continuous F₀-measurable function η₀ = F(U)—including neural networks with continuous activations—the constrained strategy obtained by projecting the network output onto K preserves continuity. The risk becomes a function of the difference between G(U) and this projected η(U), and I'm computing that this risk equals infinity unless η equals G almost everywhere.

To verify this, I'm integrating over the parameter space and examining what happens for each fixed u: if the maximum value of (G(u) − η(u))ᵀu(θ) across the constraint set is positive, then the expectation diverges due to the exponential growth in V. This means finite risk forces the difference between G and η to be non-positive across all constrained parameters for almost every u.

Now I'm checking whether this constraint forces η to equal G exactly, or if there's flexibility. Since G lies on an arc and η must be in the feasible set K, the condition (G − η)ᵀu(θ) ≤ 0 for all θ in the cone directions is equivalent to requiring η − G to lie in the dual cone. I'm computing this dual cone: it's the set of directions y where yᵀu(θ) ≥ 0 for all θ in the original cone, which gives me directions with angles in a specific range. So η must lie in the intersection of G plus this dual cone with the feasible set K, and I'm now checking whether G itself satisfies these constraints. >) ≥ 0 ⇒ |G + y|² > 1 for any nonzero y in that angular range, pushing all such points outside K. So there's no way to find a point in K that differs from G by a vector in C*.

Now I'm checking whether G could actually be equal almost everywhere to some continuous function. The issue is that φ takes on two distinct values—π/6 and π/3, differing by π/6—on interleaved sets of positive measure. If some continuous function η equaled G almost everywhere, then at density points of each set, η would have to simultaneously take on both values, which is impossible for a continuous function. The essential oscillation of G on every interval forces a contradiction. ified by a Komlós-type closedness argument, and checking that the gains are bounded by V so tightness holds. The entropic condition A3 satisfies half-bounded Fatou, and A2, A6 check out, though A1 fails. Now I need to verify whether π(−Z) is actually finite: π ≤ 0 and bounded below, with gains ≤ V, so using Jensen's inequality on the exponential moment gives π ≥ ρ(upper bound on wealth) > −∞.

For the measurability piece, Z is F_T-measurable since it's a function of U, Θ, and V, where I define the information structure I₁ := (V, Θ) so that S₁ = S₀ + Vu(Θ) is measurable and Z = G(U)ᵀ(S₁−S₀) follows. Now I need to double-check the sharpness example to make sure the activation function is bounded, measurable, and non-constant.

For the counterexample, I'm restricting to continuous bounded activations like sigmoid to keep things tractable, though I should note that discontinuous activations make the class unwieldy but don't change the core message. The key constraint is that η = G almost everywhere where η = P_K(F(U)) for any network F. I'm wondering whether all bounded measurable activations can work—specifically, if a network with binary activation like Heaviside could realize the two-valued functions that G takes on the sets A and A^c.

With a single hidden layer using Heaviside activation, the network produces piecewise constant functions with finitely many jumps, which can only equal G almost everywhere if A itself is almost everywhere a finite union of intervals. But my fat-Cantor-type set A isn't structured that way, so this fails. Even with deeper networks using Heaviside, the compositions remain piecewise constant with finitely many pieces—each layer produces finitely many indicator functions of half-lines, and subsequent layers applied to those still yield finitely many pieces. So Heaviside networks can't represent G either.

Now with general bounded measurable activations like σ = 1_ℚ, things get more subtle. A pathological activation could encode weird Borel sets, allowing networks to realize affine preimages of those sets. In principle, if A were chosen adversarially against σ, it might not be representable. But σ is fixed first, then I choose A afterward—so I need to find a set A that lies outside the family of representable sets for that σ. The representable sets form a family of at most continuum cardinality, and the measurable sets modulo null sets also have continuum cardinality, so a cardinality argument alone doesn't immediately give me a gap. I need to think about the measure-theoretic structure more carefully. The counterexample actually rules out all continuous approximation strategies, not just neural networks — the issue is fundamentally about approximating under zero slack. Let me verify the slack argument more carefully: finite risk requires (G−η)ᵀu(θ) ≤ 0 almost everywhere, which I derived by noting that if the maximum over θ of this quantity is positive, continuity forces it positive on an open interval, leading to infinite exponential moment contributions that blow up the overall expectation. Even when the maximum is non-positive for almost every u, the constraint still holds, and conversely when η equals G the risk vanishes.

Now checking the network strategy class: unconstrained networks with the composition H∘δ = P_K(F(U)) are continuous, so all such strategies yield infinite risk. The lower bound π(−Z) ≤ 0 is achieved by the measurable strategy δ = G(U), which gives H∘δ = P_K(G) = G since G already lies in K, making the objective equal to ρ(0) = 0.

The writeup will present the main convergence theorem under A1–A6, then establish sharpness by showing that dropping A1 admits a counterexample, directly answering whether the assumptions are tight. For the additional assumptions question, I'll emphasize that nothing beyond A1–A6 is needed and clarify how the A-framework and B-framework relate: they're incomparable since A requires bounded Z while B allows unbounded with exponential moments, and they differ on position limits versus nonnegativity constraints. Finally, I'll address strategy convergence in the next section.](streamdown:incomplete-link)